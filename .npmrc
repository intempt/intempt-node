# Prevent supply-chain attacks: refuse versions published in the last 7 days,
# so a compromised release cannot enter `npm ci` before it is noticed.
min-release-age=7

# Stop `npx` from silently downloading packages that are not in node_modules.
# Equivalent to passing `--no` to every npx invocation.
yes=false
